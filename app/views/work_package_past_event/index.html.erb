<%= work_package_description_with_markup( @work_package ) %>

<%= list_item_count_sentence( 'past event', @work_package_events.size ) %>
	
<% unless @work_package_past_events.empty? -%>
	<ol>
		<%= render :partial => 'work_package_event/date', :collection => @work_package_past_events, :locals => { :work_packages => @work_package_past_events } %>
	</ol>
<% end -%>

<%= render :partial => 'common/identifier', :object => @work_package %>